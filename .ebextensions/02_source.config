container_commands:
  compile:
    command: "PUPPETEER_SKIP_DOWNLOAD=true npm i && npm i dotenv && npm run prisma-generate && PUPPETEER_SKIP_DOWNLOAD=true npm run build"
  migrate:
    command: "PUPPETEER_SKIP_DOWNLOAD=true npm i && npm i dotenv && npm run prisma-migrate"
